Minimal Angular scaffold for KooliProjekt Web API.

Usage:
1. cd KooliProjektAngular
2. npm install
3. npx ng serve --proxy-config proxy.conf.json

The proxy forwards `/api` to `http://localhost:5086` (Web API).
Components: `home`, `projects`, `project-edit`. Service: `ProjectService`.
